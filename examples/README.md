# Example Knowledge Database

`fake_knowledge.sqlite3` is a small fabricated SQLite database for demos and GitHub uploads.

It does not contain personal notes, paper excerpts, local file paths, logs, or model outputs from the real local knowledge base. The live application still uses `data/db/knowledge.sqlite3` by default, and that runtime data is ignored by Git.
